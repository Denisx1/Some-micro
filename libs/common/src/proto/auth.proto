syntax = "proto3";

package auth;

service AuthService {
  rpc generateActionToken(getActionTokenRequest) returns (generateActionTokenResponse);
}

message generateActionTokenResponse {
  string token = 1;
}
message getActionTokenRequest{
  string email = 1;
  string userName = 2;
  string actionType = 3;
}